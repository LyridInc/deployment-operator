apiVersion: apps.lyrid.io/v1alpha1
kind: Subdomain
metadata:
  labels:
    app.kubernetes.io/name: lyrid-operator
    app.kubernetes.io/managed-by: kustomize
  name: subdomain-sample
spec:
  # TODO(user): Add fields here
