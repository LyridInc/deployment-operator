apiVersion: apps.lyrid.io/v1alpha1
kind: DeploymentEndpoint
metadata:
  labels:
    app.kubernetes.io/name: lyrid-operator
    app.kubernetes.io/managed-by: kustomize
  name: deploymentendpoint-sample
spec:
  # TODO(user): Add fields here
